const clock=document.querySelector("#osaka-clock");function tick(){clock.textContent=new Intl.DateTimeFormat("ja-JP",{timeZone:"Asia/Tokyo",hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false}).format(new Date())}tick();setInterval(tick,1000);
const stage=document.querySelector(".particles");for(let i=0;i<36;i++){const p=document.createElement("i");p.className="particle";p.style.left=Math.random()*100+"%";p.style.top=30+Math.random()*90+"%";p.style.setProperty("--d",8+Math.random()*14+"s");p.style.setProperty("--o",.25+Math.random()*.65);p.style.animationDelay=-Math.random()*16+"s";stage.appendChild(p)}
const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const items=document.querySelectorAll(".reveal");if(reduced){items.forEach(x=>x.classList.add("visible"))}else{const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}}),{threshold:.13});items.forEach(x=>observer.observe(x))}
const glow=document.querySelector(".cursor-glow");addEventListener("pointermove",e=>{glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"},{passive:true});